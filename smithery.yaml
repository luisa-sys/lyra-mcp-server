# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml
startCommand:
  type: http
  configSchema:
    type: object
    properties:
      url:
        type: string
        default: "https://mcp.checklyra.com/mcp"
        description: "Lyra MCP server endpoint URL"
  url: "https://mcp.checklyra.com/mcp"
